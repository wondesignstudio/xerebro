# Supabase public project URL (used on server and client)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anon key (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI API key (server-only)
OPENAI_API_KEY=

# OpenAI model (server-only)
OPENAI_MODEL=gpt-4o-mini

# Resend API key (server-only)
RESEND_API_KEY=

# Resend verified sender email
RESEND_FROM_EMAIL=onboarding@resend.dev

# Supabase service role key (server-only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# Base URL for dashboard links (e.g. https://app.xerebro.com)
APP_BASE_URL=

# Cron secret for securing scheduled API routes
CRON_SECRET=
